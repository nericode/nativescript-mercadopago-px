export class Options {
    /**
     * Only set the native iOS UINavigationController
     * */
    controller?;

    /**
     * Only set language in iOS
     * */
    language?: string = "es";

    /**
     *set publicKey
     * */
    publicKey: string;

    /**
     * set preferenceId
     * */
    preferenceId: string;
}
