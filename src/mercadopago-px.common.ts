export class Options {
    /**
     * Only set language in iOS
     * */
    language?: string = "es";

    /**
     *set publicKey
     * */
    publicKey: string;

    /**
     * set preferenceId
     * */
    preferenceId: string;
}
